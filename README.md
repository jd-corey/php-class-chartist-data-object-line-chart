# php-class-chartist-data-object-line-chart
This PHP class wraps data so that it can be visualized in a line chart (e.g. prices of products per retailer and day) by using Chartist JavaScript library.
